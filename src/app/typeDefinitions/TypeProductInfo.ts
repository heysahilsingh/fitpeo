export type TypeProductInfo = {
    id: string | number,
    title: string,
    description: string,
    images: string[],
    price: number,
    stock: number,
    soldSoFar: number,
    rating: number
}